name: Code cov for Unit Tests (x86-64)
on: [pull_request, merge_group]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
env:
  RUSTFLAGS: "-Cinstrument-coverage"
  LLVM_PROFILE_FILE: "ch-%p-%m.profraw"

jobs:
  build:
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        runner: [ 'garm-jammy' ]
        libc: [ 'gnu' ]
    name: Code cov for unit tests (x86-64)
    runs-on: ${{ github.event_name == 'pull_request' && !(matrix.runner == 'garm-jammy' && matrix.libc == 'gnu') && 'ubuntu-latest' || format('{0}-16', matrix.runner) }}
    steps:
      - name: Code checkout
        if: ${{ github.event_name != 'pull_request' || (matrix.runner == 'garm-jammy' && matrix.libc == 'gnu') }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
#      - name: Install Docker
#        if: ${{ github.event_name != 'pull_request' || (matrix.runner == 'garm-jammy' && matrix.libc == 'gnu') }}
#        run: |
#          sudo apt-get update
#          sudo apt-get -y install ca-certificates curl gnupg
#          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o -y /usr/share/keyrings/docker-archive-keyring.gpg
#          sudo chmod a+r /usr/share/keyrings/docker-archive-keyring.gpg
#          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
#          sudo apt-get update
#          sudo apt install -y docker-ce docker-ce-cli
      - name: Run unit tests
        if: ${{ github.event_name != 'pull_request' || (matrix.runner == 'garm-jammy' && matrix.libc == 'gnu') }}
        run: scripts/dev_cli.sh tests --unit --libc ${{ matrix.libc }}
      - name: Upload coverage reports to Codecov
        if: ${{ github.event_name != 'pull_request' || (matrix.runner == 'garm-jammy' && matrix.libc == 'gnu') }}
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
      - name: Skipping build for PR
        if: ${{ github.event_name == 'pull_request' && matrix.runner != 'garm-jammy' && matrix.libc != 'gnu' }}
        run: echo "Skipping build for PR"
